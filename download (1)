export interface SalesPageConfig {
  pixelId: string;
  checkoutBasico: string;
  checkoutCompleto: string;
  priceBasico: string;
  priceCompleto: string;
  oldPriceBasico: string;
  oldPriceCompleto: string;
  headline: string;
  subheadline: string;
  alertBannerText: string;
  whatsAppLink: string;
}

export interface BuyerNotification {
  name: string;
  city: string;
  plan: string;
}
